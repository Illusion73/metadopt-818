<h1>Welcome to the app/views/metaverses/index </h1>



<div class="container">
  <div class="row">
    <% @metaverses.each do |metaverse| %>
        <%= link_to metaverse_path(metaverse), class: "button col-4 mb-5 " do %>
            <div class="card" style="width: 25rem;">
              <img class="card-img-top rounded-top" src=<%= metaverse.principal_picture%> alt="Card image cap">
              <div class="card-body">
                <h5 class="card-title"><%= metaverse.title %></h5>
                <p class="card-text"><%= metaverse.price %>€</p>
              </div>
            </div>
        <% end %>
    <%end%>
  </div>
</div>
