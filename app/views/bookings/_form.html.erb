<div class="card-booking-meta-show">
  <h3 class="title mb-3">Book it !</h3>
  <h5 class="price mb-3">€ <%= @metaverse.price %> / jour</h5>
  <%= simple_form_for([@metaverse, @booking]) do |f| %>
    <%= f.date_field :start_at, class: "items form-control mb-4" %>
    <%= f.date_field :end_at, class: "items form-control mb-4" %>
    <%= f.button :submit, class: "btn btn-success" %>
  <% end %>
</div>
